<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nrutyashree Dance Academy</title>
    <link rel="icon" type="image/x-icon" href="../images/nda-favicon-v2.png">
    <style>
        body, html {
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            height: 100%;
            overflow: hidden;
        }

        .container {
            height: 100%;
            background: linear-gradient(135deg, #8a2be2, #4169e1);
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
            overflow: hidden;
        }

        /* Enhanced background with floating particles */
        .particle {
            position: absolute;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.3);
            animation: float 15s infinite linear;
        }

        /* Light beam animations */
        .light-beam {
            position: absolute;
            background: linear-gradient(to right, rgba(255,255,255,0), rgba(255,255,255,0.3), rgba(255,255,255,0));
            height: 80%;
            width: 200px;
            transform: rotate(45deg);
            filter: blur(5px);
            z-index: 2;
            animation: lightMove 8s linear infinite;
            opacity: 0;
        }

        .content {
            text-align: center;
            background: rgba(255, 255, 255, 0.9);
            padding: 2rem;
            border-radius: 12px;
            max-width: 320px;
            box-shadow: 
                0 5px 25px rgba(0, 0, 0, 0.2),
                0 0 20px rgba(138, 43, 226, 0.3);
            position: relative;
            z-index: 10;
            animation: fadeIn 0.8s ease-out;
            backdrop-filter: blur(5px);
            border: 1px solid rgba(255, 255, 255, 0.5);
        }

        .logo-container {
            position: relative;
            display: inline-block;
            margin-bottom: 1.2rem;
        }

        .logo {
            width: 120px;
            object-fit: scale-down;
            animation: gentlePulse 4s ease-in-out infinite;
            filter: drop-shadow(0 0 5px rgba(138, 43, 226, 0.5));
        }

        .logo-glow {
            position: absolute;
            top: 50%;
            left: 50%;
            width: 100%;
            height: 100%;
            transform: translate(-50%, -50%);
            background: radial-gradient(circle, rgba(138, 43, 226, 0.2) 0%, rgba(65, 105, 225, 0) 70%);
            border-radius: 50%;
            animation: glowPulse 4s ease-in-out infinite;
            z-index: -1;
        }

        /* NEW ANIMATED HEADING STYLES */
        h1 {
            margin-bottom: 8px;
            font-size: 2.2rem;
            letter-spacing: 1px;
            position: relative;
            display: inline-block;
            animation: textColorAnimation 8s;
            background-image: linear-gradient(
                90deg, 
                #ff00cc, #8a2be2, #4169e1, #00bfff, #4169e1, #8a2be2, #ff00cc
            );
            background-size: 400% 100%;
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
            
        }

        /* Text glow effect */
        h1::after {
            content: "Coming Soon";
            position: absolute;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            filter: blur(8px);
            opacity: 0.7;
            background-image: linear-gradient(
                90deg, 
                #ff00cc, #8a2be2, #4169e1, #00bfff, #4169e1, #8a2be2, #ff00cc
            );
            background-size: 400% 100%;
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: textColorAnimation 5s;
        }

        @keyframes textColorAnimation {
            0% {
                background-position: 0% 50%;
            }
            50% {
                background-position: 100% 50%;
            }
            100% {
                background-position: 0% 50%;
            }
        }

        p {
            color: #555;
            margin-bottom: 20px;
            line-height: 1.5;
            font-size: 0.95rem;
        }

        form {
            display: flex;
            flex-direction: column;
            position: relative;
        }

        input[type="email"] {
            padding: 0.8rem 1rem;
            border-radius: 8px;
            border: 2px solid rgba(138, 43, 226, 0.3);
            margin-bottom: 1rem;
            transition: all 0.3s ease;
            font-size: 15px;
            background: rgba(255, 255, 255, 0.9);
            box-shadow: 0 0 10px rgba(65, 105, 225, 0.1) inset;
        }

        input:hover, input:focus {
            border-color: #8a2be2;
            outline: none;
            box-shadow: 0 0 15px rgba(138, 43, 226, 0.25);
        }

        button {
            padding: 0.8rem;
            background: linear-gradient(90deg, #8a2be2, #4169e1);
            color: #ffffff;
            border: none;
            border-radius: 8px;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(97, 131, 235, 0.3);
            font-weight: 600;
            font-size: 15px;
            letter-spacing: 0.5px;
            cursor: pointer;
            position: relative;
            overflow: hidden;
        }

        button:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(97, 131, 235, 0.4);
            border-radius: 40px;
        }

        button:active {
            transform: translateY(-1px);
        }

        button::after {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 5px;
            height: 5px;
            background: rgba(255, 255, 255, 0.5);
            opacity: 0;
            border-radius: 100%;
            transform: scale(1, 1) translate(-50%);
            transform-origin: 50% 50%;
        }

        /* Dancing notes around the form */
        .music-note {
            position: absolute;
            font-size: 20px;
            color: rgba(138, 43, 226, 0.6);
            animation: flyNote 10s linear infinite;
            z-index: 5;
        }

        /* Animations */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes gentlePulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.08); }
        }

        @keyframes glowPulse {
            0%, 100% { transform: translate(-50%, -50%) scale(1); opacity: 0.5; }
            50% { transform: translate(-50%, -50%) scale(1.3); opacity: 0.8; }
        }

        @keyframes lightMove {
            0% { 
                left: -300px;
                top: -300px;
                opacity: 0;
            }
            20% {
                opacity: 0.5;
            }
            80% {
                opacity: 0.5;
            }
            100% { 
                left: 120%;
                top: 120%;
                opacity: 0;
            }
        }

        @keyframes ripple {
            0% {
                opacity: 0;
            }
            20% {
                opacity: 1;
            }
            100% {
                opacity: 0;
                transform: scale(20, 20) translate(-50%);
            }
        }

        @keyframes float {
            0% {
                transform: translateY(0) rotate(0deg);
                opacity: 0;
            }
            10% {
                opacity: 1;
            }
            90% {
                opacity: 1;
            }
            100% {
                transform: translateY(-1000px) rotate(720deg);
                opacity: 0;
            }
        }

        @keyframes flyNote {
            0% {
                transform: translate(0, 0) rotate(0deg);
                opacity: 0;
            }
            10% {
                opacity: 1;
            }
            90% {
                opacity: 1;
            }
            100% {
                transform: translate(var(--end-x), var(--end-y)) rotate(var(--rotation));
                opacity: 0;
            }
        }
    </style>
</head>
<body>
    <div class="container">
         <!-- Light beam animations (enhanced) -->
        <!-- <div class="light-beam" style="animation-delay: 0s;"></div> -->
        
        <!-- Floating particles -->
        <script>
            // Create floating particles on load
            window.onload = function() {
                const container = document.querySelector('.container');
                for (let i = 0; i < 20; i++) {
                    const size = Math.random() * 10 + 5;
                    const particle = document.createElement('div');
                    particle.classList.add('particle');
                    particle.style.width = `${size}px`;
                    particle.style.height = `${size}px`;
                    particle.style.left = `${Math.random() * 100}%`;
                    particle.style.bottom = '0';
                    particle.style.opacity = `${Math.random() * 0.6 + 0.2}`;
                    particle.style.animationDuration = `${Math.random() * 10 + 10}s`;
                    particle.style.animationDelay = `${Math.random() * 5}s`;
                    container.appendChild(particle);
                }
            };
        </script>
        
        <div class="content">
            <div class="logo-container">
                <div class="logo-glow"></div>
                <img src="../images/natraj-logo.png" alt="Nrutyashree Dance Academy" class="logo">
            </div>
            <h1>Coming Soon</h1>
            <p>We're choreographing something extraordinary for you. Be the first to know when we unveil our new dance experience!</p>
            <form id="notify-form" method="post" action="/enrolled">
                <input type="email" name="email" id="email" placeholder="Enter your Email" required>
                <button type="submit">Notify Me</button>
            </form>
        </div>
    </div>
</body>
</html>